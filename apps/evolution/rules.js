/* ════════════════════════════════════════════════════════════════
 *  rules.js — 외계 행성 진화 게임의 «규칙» 전부
 *  2026 광주하남영재교육원 · 중학 융합과정 (변석환) · 18차시
 *
 *  이 파일에는 서버도 화면도 없다. 순수한 규칙만 있다.
 *  학생 화면·선생님 화면·빔 화면이 똑같이 읽어서 값이 어긋나지 않는다.
 *
 *  ══ 학생에게 «정답표» 를 주지 않는다 ═══════════════════════════
 *  어느 구역에서 무엇이 유리한지는 화면 어디에도 나오지 않는다.
 *  학생은 죽고 살아남는 것을 보고 스스로 알아내야 한다.
 *  그래서 fav 표는 여기(코드)에만 있고, 화면으로는 절대 내보내지 않는다.
 *
 *  모듈을 쓰지 않는다. window.RULES 로 올린다.
 * ════════════════════════════════════════════════════════════════ */

(function (전역) {
  'use strict';

  /* ── 유전자 18개 ──────────────────────────────────────────────
     값 2 = 순종우성(FF) · 1 = 잡종(Ff) · 0 = 순종열성(ff)

     종류가 넷이다.
       보임   — 그림에 드러난다. 남의 개체도 눈으로 보고 고를 수 있다
       색     — 셋이 모여 «밝기» 하나를 만든다. 우열이 아니라 «쌓임» 이다
       숨음   — 그림에 안 드러난다. 교배해 봐야, 살아 봐야 안다
       새형질 — 처음에는 «모두 0» 이다. 오직 돌연변이로만 생긴다
                (부모에게 없던 것이 자손에게 나타나는 것을 보여 주려는 자리다) */
  var GENES = [
    /* ── 보이는 형질 ── */
    { k:'fur',   n:'털',          s:'F', d:'두꺼움', r:'얇음',   보임:true,
      뜻:'몸을 덮은 털의 두께. 두꺼우면 열이 잘 빠져나가지 않지만, 더운 곳에서는 짐이 되고 물에 젖으면 무거워집니다.',
      과학:'두꺼운 털은 털 사이사이에 공기를 가둡니다. 공기는 열을 잘 전하지 않아서, 몸에서 빠져나가는 열을 붙잡아 둡니다 — 이것을 단열이라고 합니다. 얇은 털은 열을 그대로 내보내므로 몸이 빨리 식습니다.',
      유리:'추운 곳 · 바람이 세게 부는 곳',
      불리:'더운 곳 (쌓인 열을 버리지 못합니다) · 물속 (젖으면 공기층이 사라져 단열이 안 되고 무거워집니다)' },

    /* ══ 색 — 유전자 둘, 아홉 빛깔 ═══════════════════════════════
       유전자 둘이 각각 0·1·2 라 3 × 3 = 아홉 가지가 나온다.
       그 아홉이 «구역 아홉 개의 색 그 자체» 다 — 눈빛·잿빛·짙은 초록·
       풀빛·바위빛·숲빛·모래빛·밀림빛·물빛.
       그래서 어느 구역에나 «딱 맞는 색» 이 하나씩 있고, 배경에 가까울수록
       더 산다(위장).

       색① 은 «추운 쪽 ↔ 더운 쪽», 색② 는 «마른 쪽 ↔ 습한 쪽» 을 따라간다.
       판이 왼→오른 더워지고 위→아래 습해지므로, 색 유전자 둘은 곧
       «판 위의 어디에 맞춰진 몸인가» 를 뜻한다.

       우열이 없다 — 값 그대로가 뜻이다.
       (색소 셋에 유전자 여섯이던 판은 너무 복잡해서 되돌렸다) */
    { k:'c1', n:'색 유전자 ①', s:'C', d:'큰 값', r:'작은 값', 보임:true, 색:1,
      뜻:'몸빛깔을 정하는 유전자 둘 가운데 하나. 값이 커질수록 «더운 쪽 구역» 의 색이 됩니다.',
      과학:'색은 유전자 하나가 정하지 않습니다. 둘이 함께 정합니다 — ① 이 큰 갈래를, ② 가 작은 갈래를 고르는 식입니다. 그래서 두 유전자를 엮으면 아홉 가지 빛깔이 나옵니다. 사람의 눈동자 색도 여러 유전자가 함께 정합니다.',
      유리:'몸빛깔이 그 구역의 배경과 같을 때 — 눈에 띄지 않습니다(위장)',
      불리:'배경과 다른 색이면 멀리서도 눈에 띕니다' },
    { k:'c2', n:'색 유전자 ②', s:'D', d:'큰 값', r:'작은 값', 보임:true, 색:2,
      뜻:'몸빛깔을 정하는 유전자 둘 가운데 하나. 값이 커질수록 «습한 쪽 구역» 의 색이 됩니다.',
      과학:'색 유전자 ① 과 함께 몸빛깔을 정합니다. 어느 하나만으로는 색이 정해지지 않습니다 — 둘을 같이 봐야 아홉 가지 중 하나가 나옵니다.',
      유리:'몸빛깔이 그 구역의 배경과 같을 때',
      불리:'배경과 다른 색이면 눈에 띕니다' },

    { k:'eye',   n:'눈',          s:'E', d:'큰 눈',  r:'작은 눈', 보임:true,
      뜻:'눈의 크기. 크면 멀리까지, 어두운 곳까지 봅니다. 대신 강한 빛과 모래바람에 약합니다.',
      과학:'눈이 크면 빛을 받아들이는 면이 넓어져, 어두운 곳에서도 보이고 멀리까지 봅니다. 대신 크고 축축한 눈은 다치기 쉽고, 강한 빛에는 눈이 부십니다.',
      유리:'어두운 곳 · 멀리까지 봐야 하는 트인 곳 · 천적이 있는 곳',
      불리:'모래바람이 부는 곳 · 볕이 아주 강한 곳' },
    { k:'leg',   n:'다리',        s:'L', d:'많음',   r:'적음',   보임:true,
      뜻:'다리 수. 많으면 빠르고 잘 넘어지지 않습니다. 대신 무른 땅과 물속에서는 걸리적거립니다.',
      과학:'다리가 많으면 몸무게가 여러 곳으로 나뉘고 딛는 점이 늘어서, 빨리 달리고 잘 넘어지지 않습니다. 대신 다리마다 힘을 써야 하고 물속에서는 물의 저항을 받습니다.',
      유리:'달아나야 하는 트인 곳 · 발이 빠지는 무른 땅 · 발 디딜 곳이 좁은 바위',
      불리:'모래처럼 아주 무른 곳 (많은 발이 오히려 자꾸 빠집니다) · 물속 (헤엄에 걸리적거립니다)' },
    { k:'tall',  n:'키',          s:'T', d:'큼',     r:'작음',   보임:true,
      뜻:'몸집과 키. 크면 높은 곳에 닿고 멀리 봅니다. 대신 좁은 곳을 못 지나고 바람과 물살을 그대로 받습니다.',
      과학:'키가 크면 높은 곳에 닿고 멀리 봅니다. 대신 몸이 크면 겉넓이도 늘어 열을 빨리 잃거나 얻고, 바람과 물살을 받는 면도 넓어집니다. 좁은 틈은 지나가지 못합니다.',
      유리:'먹이가 높이 달린 곳 · 풀 너머를 봐야 하는 곳',
      불리:'바람이 낮게 깔리는 곳 · 나뭇가지가 촘촘한 곳 · 물살이 센 곳 · 땅이 흔들릴 때' },
    { k:'water', n:'물저장 조직', s:'W', d:'있음',   r:'없음',   보임:true,
      뜻:'몸 안에 물을 담아 두는 주머니. 있으면 마른 곳에서 버팁니다. 대신 무게가 늘어납니다. — 많다고 늘 좋은 것은 아닙니다.',
      과학:'몸 안에 물을 담아 두는 주머니입니다. 마른 곳에서 물 없이 버티게 해 주지만, 물은 무거워서 담은 만큼 몸이 굼떠집니다. 담는 양과 무게 사이에 주고받기가 있습니다.',
      유리:'물을 오래 못 마시는 곳',
      불리:'많이 담을수록 무거워집니다 — 있고 없고보다 «얼마나» 가 중요한 형질입니다' },
    { k:'grip',  n:'발톱',        s:'G', d:'강함',   r:'약함',   보임:true,
      뜻:'발끝의 갈고리. 바위와 나뭇가지를 단단히 붙잡습니다.',
      과학:'발끝의 갈고리입니다. 표면의 오톨도톨한 곳에 걸려 마찰을 크게 만들어, 미끄러운 데서도 몸을 붙잡아 둡니다.',
      유리:'깎아지른 바위 · 나뭇가지 · 땅이 흔들리는 때 · 세게 부딪히는 충격',
      불리:'헤엄칠 때는 붙잡을 것이 없어 쓸모가 없습니다' },

    /* 팔과 지느러미는 «같은 자리» 다 — 앞다리 하나를 무엇으로 쓰느냐다.
       AA=팔 · aa=지느러미 · Aa=지금 있는 곳에서 더 쓸모 있는 쪽이 드러난다.
       같은 유전자형인데 사는 곳에 따라 겉모습이 달라진다 — 반응 규준 */
    { k:'limb',  n:'앞다리',      s:'A', d:'팔',     r:'지느러미', 보임:true, 조건부:true,
      뜻:'앞다리 하나를 «팔» 로 쓸지 «지느러미» 로 쓸지 정합니다. 잡종(Aa)은 지금 사는 곳에서 더 쓸모 있는 쪽으로 자랍니다.',
      과학:'팔과 지느러미는 서로 다른 기관이 아니라 같은 앞다리가 다르게 자란 것입니다(상동기관 — 사람의 팔과 고래의 앞지느러미가 그렇습니다). 그래서 한 유전자가 둘을 함께 정합니다. 잡종(Aa)은 자라는 동안 둘레 환경을 읽어 더 쓸모 있는 쪽으로 자랍니다 — 유전자형은 그대로인데 사는 곳이 바뀌면 겉모습이 바뀝니다.',
      유리:'팔 → 나무 위·붙잡고 옮겨 다녀야 하는 곳 / 지느러미 → 물속',
      불리:'팔은 물에서, 지느러미는 땅에서 짐이 됩니다. 잡종은 옮겨 다니며 바꿔 쓸 수 있습니다' },

    { k:'ant',   n:'더듬이',      s:'M', d:'긺',     r:'짧음',   보임:true,
      뜻:'소리·냄새·떨림을 느끼는 감각기관. 눈이 잘 안 보이는 곳에서 눈 대신 씁니다. 아주 길면 다가오는 재해를 남보다 먼저 알아챕니다.',
      과학:'소리 · 냄새 · 공기의 떨림을 느끼는 감각기관입니다. 눈이 빛에 기대는 것과 달리, 더듬이는 어두워도 씁니다. 아주 길면 멀리서 오는 흔들림까지 잡아내, 재해가 닥치기 전에 알아챕니다.',
      유리:'어두운 곳 · 그늘이 짙은 곳 · 물속 · 천적이 있는 곳 · 재해가 잦은 곳',
      불리:'모래바람이 부는 곳 (길게 뻗은 것이 쓸립니다)' },

    /* ── 숨은 형질 ── */
    { k:'life',  n:'수명',        s:'V', d:'긺',     r:'짧음',   보임:false,
      뜻:'한 세대를 더 사는 힘. 오래 살면 교배를 두 번 하지만, 그만큼 몸에 부담이 됩니다.',
      과학:'한 세대를 더 사는 힘입니다. 오래 사는 몸은 손상을 고치는 데 힘을 쓰므로, 그만큼 다른 데 쓸 힘이 줄어 그 세대의 생존은 조금 불리해집니다. 대신 살아남으면 교배를 한 번 더 합니다.',
      유리:'먹이가 드물어 자손을 자주 못 남기는 곳 (춥거나 메마른 곳)',
      불리:'해마다 위험이 큰 곳 — 오래 살 기회 자체가 오지 않습니다' },
    { k:'fec',   n:'번식력',      s:'R', d:'높음',   r:'낮음',   보임:false,
      뜻:'한 번 교배할 때 낳는 자손 수. 높을수록 네 마리를 낳을 확률이 올라갑니다.',
      과학:'한 번 교배할 때 낳는 자손 수입니다. 이 형질도 우열의 법칙을 따르지 않습니다 — 대립유전자가 쌓이는 만큼 확률이 오릅니다. 그래서 잡종도 순종열성보다 낫습니다.',
      유리:'자손을 많이 낳으면 그만큼 다양한 유전자가 나옵니다',
      불리:'한 마리에 들이는 힘은 줄어듭니다. 자리가 좁으면 서로 붐벼 다 같이 손해입니다' },
    { k:'heat',  n:'내열',        s:'H', d:'강함',   r:'약함',   보임:false,
      뜻:'몸속에 쌓이는 열을 견디는 힘. 겉으로는 전혀 드러나지 않습니다.',
      과학:'몸속 단백질이 열에 풀어지지 않게 버티는 힘입니다. 세포 안에서 열을 견디게 돕는 물질을 얼마나 만드느냐로 정해집니다.',
      유리:'더운 곳 · 화산처럼 뜨거운 것이 덮치는 때',
      불리:'만드는 데 힘이 들어, 서늘한 곳에서는 그 힘이 낭비가 됩니다' },
    { k:'tox',   n:'내독',        s:'X', d:'강함',   r:'약함',   보임:false,
      뜻:'독과 나쁜 공기를 견디는 힘. 겉으로는 전혀 드러나지 않습니다.',
      과학:'독과 나쁜 공기를 몸 안에서 풀어 없애는 힘입니다(해독). 간처럼 독을 걸러 내는 기관이 얼마나 잘 도느냐로 정해집니다.',
      유리:'쓴 것을 먹어야 하는 곳 · 물속 · 화산재나 먼지가 덮치는 때',
      불리:'해독에도 힘이 들어, 깨끗한 곳에서는 그 힘이 낭비가 됩니다' },

    /* ══ 새 형질 — 처음에는 아무도 갖고 있지 않다 ══════════════════
       모든 개체가 0(없음) 으로 시작한다. 학생도 DNA 로 켤 수 없다.
       오직 «자손이 태어날 때의 돌연변이» 로만 생긴다. 아주 드물다.
       한 번 생기면 우성이라 그 자리에서 바로 눈에 보이고,
       그 다음부터는 멘델의 법칙대로 퍼지거나 사라진다.
       — 변이가 먼저 생기고, 그 다음에 환경이 고른다는 것을 눈으로 본다

       여덟 가지다. 하나하나 «원래 있던 형질이 딴것이 된» 것으로 두었다
       (원래: 항목). 없던 기관이 허공에서 생기는 게 아니라, 있던 것이
       다른 쓸모로 바뀐다는 것 — 그게 실제 진화가 하는 일이다.
       유리한 구역이 서로 겹치지 않게 흩어 놓았다. 어느 하나가 «늘 좋은
       형질» 이 되면 학생이 환경을 볼 이유가 사라진다 */
    { k:'scale', n:'비늘',        s:'S', d:'있음',   r:'없음',   보임:true, 새형질:true,
      원래:'털', emoji:'🐊',
      뜻:'털 대신 몸을 덮는 단단한 비늘. 물을 먹지 않고 몸을 지켜 줍니다.',
      과학:'털이 자라야 할 자리에서 피부가 두껍고 단단하게 굳어 비늘이 된 것입니다. 물을 먹지 않아 젖어도 무거워지지 않고, 몸속 물이 날아가는 것도 막습니다. 부딪히는 충격도 막아 줍니다. 대신 공기를 가두지 못해 단열은 되지 않습니다.',
      유리:'물속 · 아주 마르고 더운 곳 · 무언가 부딪히는 재해',
      불리:'아주 추운 곳 (열을 붙잡아 두지 못합니다)',
      재해:{ volcano:0.12, meteor:0.12, quake:0.12 } },
    { k:'glow',  n:'빛깔',        s:'Q', d:'있음',   r:'없음',   보임:true, 새형질:true,
      원래:'색', emoji:'🌈',
      뜻:'부모에게는 없던 전혀 다른 빛깔. 보는 각도에 따라 색이 바뀝니다. 위장은 되지 않습니다.',
      과학:'색소가 아니라 껍질의 아주 얇은 층이 빛을 튕겨 내면서 생기는 색입니다(구조색). 그래서 색 유전자와 상관없이 나타나고, 색 유전자가 만든 위장을 덮어 버립니다. 번쩍이는 빛은 쫓아오는 쪽의 눈을 헷갈리게 하지만, 숨을 곳이 없는 데서는 그냥 눈에 띌 뿐입니다.',
      유리:'그늘이 얼룩덜룩해 빛이 어른거리는 곳 (숲)',
      불리:'숨을 곳 없이 트인 곳 · 배경이 한 가지 색인 곳 (위장이 아예 안 됩니다)' },
    { k:'wing',  n:'날개',        s:'Y', d:'있음',   r:'없음',   보임:true, 새형질:true,
      원래:'앞다리', emoji:'🦋',
      뜻:'앞다리에서 뻗어 나온 얇은 막. 날지는 못해도 미끄러지듯 건너뜁니다.',
      과학:'앞다리와 몸통 사이에 얇은 막이 생긴 것입니다. 떨어질 때 공기를 받아 천천히 내려오고, 높은 데서 뛰면 멀리까지 미끄러져 갑니다(활공). 위험이 닥쳤을 때 남보다 빨리 그 자리를 벗어날 수 있습니다. 대신 물에 젖으면 무겁게 늘어붙습니다.',
      유리:'높은 나무가 있는 곳 · 트인 곳 · 재해가 닥칠 때 (먼저 달아납니다)',
      불리:'물속 (젖은 막은 짐이 됩니다)',
      재해모두:0.13 },
    { k:'gill',  n:'아가미',      s:'B', d:'있음',   r:'없음',   보임:true, 새형질:true,
      원래:'물저장 조직', emoji:'💧',
      뜻:'목 옆에 난 여러 겹의 틈. 물속에서도 숨을 쉽니다.',
      과학:'물을 저장하던 주머니가 얇게 접히고 갈라져, 물이 지나가면서 그 안에 녹아 있는 산소를 걸러 쓰게 된 것입니다. 물속에서 숨을 쉴 수 있으니 물 위로 올라올 일이 없습니다. 대신 얇은 틈은 마르면 들러붙어 못 쓰게 됩니다 — 물 밖에서는 짐일 뿐입니다.',
      유리:'물속 · 물이 갑자기 차오르는 때',
      불리:'마르고 더운 곳 (아가미가 말라붙습니다)',
      재해:{ sink:0.28 } },
    { k:'horn',  n:'뿔',          s:'N', d:'있음',   r:'없음',   보임:true, 새형질:true,
      원래:'발톱', emoji:'🦌',
      뜻:'머리에서 자라 나온 단단한 뿔. 밀고 받아 냅니다.',
      과학:'발톱을 만들던 단단한 성분이 머리뼈 위에서도 자라난 것입니다. 잡아먹으려 달려드는 것을 받아 내고, 같은 종끼리 자리를 다툴 때도 씁니다. 대신 늘 머리에 얹고 다녀야 하고, 가지가 빽빽한 곳에서는 걸려서 지나가지 못합니다.',
      유리:'잡아먹는 것이 사는 곳 · 트인 벌판 · 바위산',
      불리:'가지가 빽빽한 숲 · 물속 (걸리고 물살을 받습니다)' },
    { k:'leaf',  n:'엽록 피부',   s:'P', d:'있음',   r:'없음',   보임:true, 새형질:true,
      원래:'색', emoji:'🌿',
      뜻:'피부 속에 초록 알갱이가 생겨, 햇빛으로 스스로 양분을 만듭니다.',
      과학:'피부 세포 안에 엽록체가 들어앉은 것입니다(실제 바다 민달팽이에게 일어나는 일입니다). 빛만 있으면 먹지 않고도 버티므로, 먹을 것이 없는 곳에서 오래 견딥니다. 대신 빛이 닿아야만 쓸모가 있어, 그늘지거나 깊은 곳에서는 아무 소용이 없습니다.',
      유리:'햇빛이 강하고 트인 곳 · 먹을 것이 드문 곳',
      불리:'그늘이 짙은 숲 · 물속 · 하늘이 덮이는 재해',
      재해:{ volcano:-0.06, meteor:-0.06 } },
    { k:'dig',   n:'굴 파는 발',  s:'K', d:'있음',   r:'없음',   보임:true, 새형질:true,
      원래:'발톱', emoji:'🕳️',
      뜻:'넓적한 삽 모양 앞발. 땅을 파고 들어갑니다.',
      과학:'발톱이 뾰족하게 자라는 대신 넓적하게 퍼진 것입니다. 땅속은 낮에도 서늘하고 밤에도 덜 추워서, 굴에 들어가 있으면 바깥 날씨를 거의 안 겪습니다. 위에서 무엇이 떨어져도 굴이 막아 줍니다. 대신 무른 땅이 있어야 하고, 물이 차오르면 굴이 오히려 덫이 됩니다.',
      유리:'파고들 땅이 있는 트인 곳 · 무엇이 떨어지거나 흔들리는 재해',
      불리:'물속 · 나무 위 · 물이 차오르는 때 (굴에 갇힙니다)',
      재해:{ volcano:0.14, meteor:0.14, quake:0.14, rise:0.14, sink:-0.10 } },
    { k:'echo',  n:'반향정위',    s:'Z', d:'있음',   r:'없음',   보임:true, 새형질:true,
      원래:'더듬이', emoji:'🦇',
      뜻:'소리를 내쏘고 돌아오는 메아리로 앞을 «봅니다».',
      과학:'떨림을 느끼던 더듬이의 감각이 귀 쪽으로 옮겨 가, 스스로 소리를 내고 그 소리가 부딪혀 돌아오는 시간을 재는 방식으로 바뀐 것입니다(박쥐와 돌고래가 씁니다). 빛이 하나도 없어도 앞에 무엇이 있는지 알 수 있습니다. 대신 훤히 보이는 곳에서는 눈이 하는 일을 되풀이할 뿐입니다.',
      유리:'빛이 잘 안 드는 곳 · 물속 · 먼지가 앞을 가리는 때',
      불리:'훤히 트여 눈으로 다 보이는 곳 (힘만 듭니다)',
      재해:{ volcano:0.12, meteor:0.12 } }
  ];
  var NG = GENES.length;
  var 색자리=[], 새형질들=[], 편집가능=[], 보이는수=0;
  for (var _i = 0; _i < NG; _i++) {
    if (GENES[_i].보임) 보이는수++;
    if (GENES[_i].색) 색자리[GENES[_i].색 - 1] = _i;
    if (GENES[_i].새형질) 새형질들.push(GENES[_i]); else 편집가능.push(_i);
  }

  /* 아홉 빛깔 — 구역 아홉 개의 배경색 그대로.
     칸 [색①][색②] 가 그대로 판 위의 «추움→더움 × 마름→습함» 자리를 따른다 */
  var 색표 = [
    { n:'눈빛',      hex:'#cfe6ff' }, { n:'잿빛',   hex:'#b7c4cd' }, { n:'짙은 초록', hex:'#2f5d43' },
    { n:'풀빛',      hex:'#bcc96a' }, { n:'바위빛', hex:'#8a8f9a' }, { n:'숲빛',      hex:'#2e7d4f' },
    { n:'모래빛',    hex:'#e3c47e' }, { n:'밀림빛', hex:'#3a6b2f' }, { n:'물빛',      hex:'#2f7fb5' }
  ];

  /* ── 잡종이 유리한 형질 ───────────────────────────────────────
     물저장 조직만은 «잡종일 때» 적응도가 오른다.
       WW = 물주머니가 둘 — 넉넉하지만 몸이 무거워 굼뜨다
       Ww = 하나        — 딱 좋다
       ww = 없음        — 마른 곳에서 버티지 못한다
     이게 없으면 학생이 잡종을 고를 이유가 없어 멘델이 게임에서 사라진다.
     이 사실도 학생에게 알려 주지 않는다. 여러 번 낳아 보고 알아내야 한다. */
  /* 14로는 안 보였다. 사막에서는 W 자체의 효과가 너무 커서 WW 로 고정돼 버렸고
     (WW 62% : Ww 33% : ww 5%), 나머지 칸에서는 하디-바인베르크와 구분이 안 됐다.
     그래서 값을 올리고, 물저장을 «구역 유불리(fav)» 에서 전부 뺐다.
     이제 물저장은 «있고 없고» 가 아니라 «Ww 일 때가 제일 좋다» 만 남는다 */
  var HET = { water: 32 };

  /* ── 바이옴 ───────────────────────────────────────────────────
     왼→오른쪽으로 더워지고, 위→아래로 습해진다.
     바다(sea)는 아주 습한 쪽 끝에 있다 — 비가 계속 늘면 저절로 나타나고,
     «대지 침강» 사건으로 한꺼번에 생기기도 한다.

     fav = 이 구역에서 유리한 겉모습 (1=우성 쪽, 0=열성 쪽)
     w   = 그 형질의 중요도 (안 적으면 1)
     ★ 이 표는 학생에게 절대 보여 주지 않는다 ★ */
  var BIOMES = [
    { key:'snow',   name:'눈 덮인 곳',   emoji:'❄️', t:0.05, m:0.15, bg:'#cfe6ff', ink:'#123',
      설명:'숨을 쉬면 하얀 김이 납니다. 발밑은 온통 눈이라, 몸에서 새어 나가는 열을 붙잡아 두지 못하면 밤을 넘기지 못합니다. 사방이 하얘서 어두운 것은 멀리서도 눈에 띕니다. 길게 뻗은 것은 무엇이든 먼저 얼어붙습니다. 먹을 것이 드물어, 한 번 태어나면 오래 버텨야 합니다.',
      fav:{ fur:1, life:1, limb:1 },        w:{ fur:2 },
      밝기:6, 앞다리:'arm', 새:{ scale:-22, glow:-16, wing:3,  gill:-12, horn:4,   leaf:14,  dig:8,   echo:-4 } },
    { key:'tundra', name:'툰드라',       emoji:'🌫️', t:0.20, m:0.55, bg:'#b7c4cd', ink:'#123',
      설명:'안개가 걷히지 않는 벌판입니다. 바람이 낮게 깔려 지나가서 키가 큰 것은 온종일 바람을 맞습니다. 땅이 질척여 딛는 곳이 여럿이어야 빠지지 않습니다. 여전히 춥고, 배경은 흐린 잿빛입니다. 여기서도 오래 버티는 쪽이 남습니다.',
      fav:{ fur:1, tall:0, leg:1, life:1, limb:1 },                w:{ fur:1.5 },
      밝기:4, 앞다리:'arm', 새:{ scale:-10, glow:-10, wing:6,  gill:-4,  horn:6,   leaf:8,   dig:10,  echo:0 } },
    { key:'taiga',  name:'침엽수림',     emoji:'🌲', t:0.38, m:0.82, bg:'#2f5d43', ink:'#eaffe9',
      설명:'곧게 뻗은 침엽수가 빽빽합니다. 먹이는 나무 위에 있어서, 붙잡을 것과 매달릴 힘과 닿는 높이가 있어야 합니다. 그늘이 짙어 어두운 몸이 덜 띕니다. 침엽에서 나오는 진액은 씁니다. 빛이 적어, 눈보다 다른 감각이 쓸모 있습니다.',
      fav:{ tall:1, grip:1, tox:1, ant:1, limb:1 },         w:{},
      밝기:1, 앞다리:'arm', 새:{ scale:0,   glow:16,  wing:19, gill:0,   horn:-12, leaf:-14, dig:-8,  echo:18 } },
    { key:'grass',  name:'초원',         emoji:'🌾', t:0.52, m:0.22, bg:'#bcc96a', ink:'#1a1a00',
      설명:'지평선까지 탁 트인 마른 풀밭입니다. 숨을 곳이 없어, 멀리서 오는 것을 먼저 보고 빨리 달아나는 수밖에 없습니다. 풀 너머를 보려면 키도 있어야 합니다. 마른 풀은 누렇게 밝아서 밝은 몸이 묻힙니다. 낮이 뜨거워 두꺼운 것은 짐이 됩니다.',
      fav:{ leg:1, eye:1, tall:1, fur:0, limb:1 },          w:{ leg:2 },
      밝기:5, 앞다리:'arm', 새:{ scale:6,   glow:-19, wing:16, gill:-14, horn:14,  leaf:20,  dig:16,  echo:-8 } },
    { key:'forest', name:'초록 숲·천적', emoji:'🐾', t:0.58, m:0.68, bg:'#2e7d4f', ink:'#eaffe9',
      설명:'잎이 겹겹이 쌓인 초록 숲입니다. 여기에는 우리를 잡아먹는 것이 삽니다. 초록 그늘 속에서는 어두운 몸이 사라지듯 묻힙니다. 나뭇가지 사이가 좁아 몸집이 큰 것은 걸립니다. 잎 흔들리는 소리와 냄새를 먼저 알아채고, 보고, 달아나는 쪽이 삽니다.',
      fav:{ eye:1, leg:1, tall:0, ant:1, limb:1 },   w:{  eye:1.5, ant:1.5 },
      밝기:1, 앞다리:'arm', 새:{ scale:0,   glow:22,  wing:13, gill:-4,  horn:24,  leaf:-12, dig:0,   echo:16 } },
    { key:'rock',   name:'바위산',       emoji:'🪨', t:0.48, m:0.38, bg:'#8a8f9a', ink:'#111',
      설명:'깎아지른 바위가 층층이 서 있습니다. 붙잡지 못하면 미끄러져 떨어집니다. 발 디딜 곳이 좁아 여러 개로 나눠 디뎌야 합니다. 바위는 거무스름합니다. 돌 틈의 이끼에는 쓴맛이 돕니다. 먹이가 드물어 오래 버티는 쪽이 남습니다.',
      fav:{ grip:1, leg:1, tox:1, life:1, limb:1 },       w:{ grip:2 },
      밝기:2, 앞다리:'arm', 새:{ scale:13,  glow:-6,  wing:10, gill:-12, horn:12,  leaf:10,  dig:6,   echo:4 } },
    { key:'jungle', name:'높은 나무숲',  emoji:'🌴', t:0.80, m:0.80, bg:'#3a6b2f', ink:'#eaffe9',
      설명:'덥고 습한 곳에 아주 높은 나무가 솟아 있습니다. 먹을 것은 전부 꼭대기에 달려 있어서, 닿는 높이가 곧 먹는 양입니다. 가지를 붙잡고 건너다녀야 하고, 잎 틈으로 멀리 보아야 다음 가지를 찾습니다. 푹푹 쪄서 두꺼운 것은 견디기 힘듭니다.',
      fav:{ tall:1, grip:1, eye:1, fur:0, limb:1 },           w:{ tall:2 },
      밝기:1, 앞다리:'arm', 새:{ scale:3,   glow:16,  wing:22, gill:0,   horn:-14, leaf:-12, dig:-10, echo:12 } },
    { key:'desert', name:'더운 사막',    emoji:'🏜️', t:0.95, m:0.10, bg:'#e3c47e', ink:'#3a2600',
      설명:'모래가 하얗게 달아오릅니다. 그늘이 없어, 낮에는 몸속의 열을 견디는 힘이 전부입니다. 두껍고 어두운 것은 열을 그대로 받습니다. 모래바람에 눈이 시려 크게 뜨고 있기 어렵고, 길게 뻗은 것은 모래에 쓸립니다. 발이 많으면 모래에 자꾸 빠집니다.',
      fav:{ fur:0, eye:0, leg:0, heat:1, ant:0, limb:1 }, w:{ heat:1.5 },
      밝기:6, 앞다리:'arm', 새:{ scale:22,  glow:-19, wing:3,  gill:-22, horn:0,   leaf:22,  dig:20,  echo:-10 } },
    /* 바다 — 아주 습한 쪽. 헤엄치지 못하면 못 산다 */
    { key:'sea',    name:'바다',         emoji:'🌊', t:0.55, m:1.28, bg:'#2f7fb5', ink:'#eaf6ff',
      설명:'발이 닿지 않습니다. 물을 밀어낼 것이 없으면 가라앉습니다. 물속에서는 다리가 걸리적거리고, 몸이 크면 물살에 밀립니다. 털은 물을 먹어 무거워지고, 붙잡을 바위는 없습니다. 물에는 쓴 것이 녹아 있고, 빛이 적어 물결의 떨림으로 길을 찾습니다.',
      fav:{ leg:0, tall:0, fur:0, grip:0, tox:1, ant:1, limb:0 },    w:{ leg:1.5, limb:2.5 },
      밝기:2, 앞다리:'fin', 새:{ scale:22,  glow:0,   wing:-19,gill:30,  horn:-18, leaf:-8,  dig:-20, echo:14 } }
  ];

  /* ══ 재해 ══════════════════════════════════════════════════════
     세대마다 낮은 확률로 한 구역 일대를 덮친다.
     사건마다 «살아남게 해 주는 형질» 이 다르다. 이것도 알려 주지 않는다.

     기본생존 = 아무 형질도 안 맞을 때의 생존 확률
     살아남는 = 맞으면 생존 확률이 오르는 형질 (1=우성 쪽, 0=열성 쪽)
     지형     = 사건이 끝난 뒤 그 칸이 무엇으로 바뀌는가 (null 이면 그대로) */
  var EVENTS = [
    { k:'volcano', n:'화산 폭발', emoji:'🌋', 범위:2, 기본생존:0.15,
      살아남는:{ heat:1, tox:1, fur:0, ant:1 }, w:{ heat:3, tox:1.5 }, 지형:'rock',
      말:'뜨거운 재가 하늘을 덮었습니다' },
    { k:'meteor',  n:'운석 충돌', emoji:'☄️', 범위:3, 기본생존:0.12,
      살아남는:{ tox:1, grip:1, tall:0 }, w:{ tox:2.5, tall:1.5 }, 지형:null,
      말:'하늘에서 돌이 떨어져 먼지가 뒤덮었습니다' },
    { k:'quake',   n:'지진',      emoji:'🌎', 범위:4, 기본생존:0.30,
      살아남는:{ leg:1, tall:0, grip:1, ant:1 }, w:{ leg:2.5, ant:1.5 }, 지형:null,
      말:'땅이 흔들려 서 있기가 힘듭니다' },
    /* «fin» 이라고 적어 두었는데 그런 유전자는 없다(앞다리는 limb 이다).
       그래서 침강의 핵심인 «지느러미라야 산다» 가 여태 아무 일도 안 했다.
       limb 은 잡종이면 사는 곳이 정하므로, 뭍에서 팔로 자란 잡종은
       물이 차오를 때 그대로 걸린다 — 그게 더 맞다 */
    { k:'sink',    n:'대지 침강', emoji:'🌊', 범위:3, 기본생존:0.08,
      살아남는:{ limb:0, water:1 }, w:{ limb:3 }, 지형:'sea',
      말:'땅이 가라앉아 바다가 되었습니다' },
    { k:'rise',    n:'대지 융기', emoji:'⛰️', 범위:3, 기본생존:0.35,
      살아남는:{ leg:1, grip:1, limb:1 }, w:{ grip:2 }, 지형:'rise',
      말:'바다가 솟아올라 마른 땅이 되었습니다' }
  ];

  var PHASES = ['setup', 'survive', 'move', 'breed', 'next', 'env'];
  var PHASE_NAME = {
    lobby:'참가 대기', setup:'유전자 제작', survive:'생존 판정', move:'이동',
    breed:'교배', next:'세대 교체', env:'환경 변화', end:'게임 끝'
  };

  function 기본설정() {
    return {
      /* 생존 확률 = base + 적응도/100 × span
         적응도 0 → 20% · 50 → 50% · 100 → 80% */
      base:0.20, span:0.60,

      /* 수명 — 오래 사는 대신 생존 판정에서 손해를 본다.
         한 세대를 더 살면 교배를 두 번 하므로 이득이 아주 크다.
         값은 시뮬레이터로 «대립유전자 빈도가 0.5 근처» 가 되게 맞췄다. */
      /* 12세대·12명·6회로 맞춘 값.
         0.14 → 수명 63·개체 147   ·   0.17 → 수명 64·개체 131(최소 69)
         0.20 → 수명 53·개체  87   ·   0.26 → 수명 52·개체  47(너무 얇다)
         0.17 이 «수명이 정답이 되지도, 사라지지도 않으면서» 개체군이 튼튼했다 */
      lifeCost:0.17, lifeExtra:1,

      /* 번식력 — 자손은 2마리 아니면 4마리.
         4마리가 나올 확률이 유전자형에 따라 다르다 (우열이 아니라 «쌓인다»)
           rr 10%  ·  Rr 20%  ·  RR 30% */
      /* 10/20/30 은 개체 하나로는 절대 체감이 안 됐다(rr 이 RR 이상 나올 확률 73%).
         벌려 놓고, 대신 번식력을 «구역 유불리» 에서 빼서
         «생존률엔 안 나오는데 자손이 유독 많다» 가 깨끗이 드러나게 했다 */
      kid2:2, kid4:4, fec0:0.00, fec1:0.25, fec2:0.50,

      inbreed:1,       // 내 개체끼리 교배하면 자손이 이만큼 준다 (근친약세)
      /* 14로 두었더니 «친구랑 한 칸에 뭉치기» 가 압도적으로 이득이었다.
         (같은 칸 두 학생 → 자손 21.6마리 / 다른 칸 → 13.2마리, 1.6배) */
      cellCap:10,      // 한 구역이 넉넉히 먹여 살리는 수
      crowd:0.015,     // 넘으면 한 마리당 깎이는 생존 확률
      rescue:2,        // 전멸한 학생에게 보내는 이주민
      mut:0.02,        // 유전자 하나가 돌연변이할 확률
      cap:16,          // 학생 한 명의 개체 상한
      start:10,        // 시작 개체 수
      moveSec:120, breedSec:240,
      autoScope:'zone',
      /* 환경이 한 번에 움직이는 폭. 판의 칸 하나가 0.25 다.
         0.12 로는 한 세대에 25칸 중 5칸만 바뀌고, 여덟 세대에 한 번은
         아무 칸도 안 바뀌었다 — «환경이 변한다» 가 잘 안 보였다.
         0.25 로 하면 온난화 한 번이 판 전체를 정확히 한 칸씩 민다. */
      shift:0.25,      // 환경이 한 번에 움직이는 폭 (칸 하나 = 0.25)
      eventRate:0.30,  // 세대마다 재해가 일어날 확률

      /* ── DNA 크레딧 ──
         수업 전 문제 점수만큼 선생님이 넣어 준다.
         유전자를 고칠 때와 남과 거래할 때 쓴다. 상을 받으면 더 받는다.

         입양(DNA 를 내고 무작위 개체 받기)은 넣지 않았다.
         혼자 하는 일이라 아무와도 말할 필요가 없고, 같은 DNA 로
         유전자를 다섯 자리 고치는 편이 언제나 이득이라 고를 이유가 없었다. */
      /* 새 형질이 생길 확률 — 한배 새끼마다, 형질 한 자리마다.
         보통 돌연변이율(2%)을 쓰면 한 세대에 수십 마리가 생겨 «드문 일» 이
         아니게 된다.
         형질이 셋일 때 맞춘 값을 여덟으로 늘린 뒤에도 그대로 두었다.
         한 형질만 보면 여전히 한 판에 1.5번쯤으로 드물고, 대신 반 전체로는
         한 판(10세대·12명)에 11번쯤 · 서로 다른 여섯 가지가 나온다.
         세대마다 어디선가 한 번은 «없던 게 나타났다» 가 뜨는 셈이다 */
      novelMut:0.004,

      editCost:1,      // 유전자 한 자리 고치기
      scoutCost:2,     // 정찰 — 이번 세대 동안 적응도를 볼 수 있다
      awardDna:3,      // 상 하나당 주는 DNA
      tradeMax:2,      // 한 세대에 낼 수 있는 거래 신청 수
      tradeDnaMax:3    // 거래 한 건에 얹을 수 있는 DNA 상한
    };
  }

  /* ── 유전자 읽기 ─────────────────────────────────────────────
     겉모습을 읽는 방식이 형질마다 다르다.
       보통    — 우성이 하나라도 있으면 우성 (우열의 법칙)
       색 셋   — 값을 다 더한다. 우열이 없다 (쌓임)
       앞다리  — 잡종이면 «지금 사는 곳» 이 정한다 (반응 규준)
     그래서 표현형을 읽을 때 어디에 있는지(biomeKey)를 같이 넘겨야 한다 */
  function 자리(key) { for (var i=0;i<NG;i++) if (GENES[i].k===key) return i; return -1; }

  /* ── 색 ────────────────────────────────────────────────────────
     유전자 둘의 값(0·1·2)을 엮어 아홉 빛깔 가운데 하나를 고른다.
       번호 = 색① × 3 + 색②
     그 아홉은 구역 아홉 개의 배경색 그대로라, 어느 구역에나 «딱 맞는 색»
     이 하나씩 있다. 배경에 가까울수록 눈에 덜 띄어 더 산다. */
  function 색번호(geno) {
    var a = +geno.charAt(색자리[0]) || 0, b = +geno.charAt(색자리[1]) || 0;
    return Math.max(0, Math.min(8, a*3 + b));
  }
  function 빛깔(geno) { return 색표[색번호(geno)]; }
  function 몸빛깔(geno) { return _hex(색표[색번호(geno)].hex); }

  function _hex(h) {
    return [parseInt(h.slice(1,3),16), parseInt(h.slice(3,5),16), parseInt(h.slice(5,7),16)];
  }
  /* 배경과 얼마나 다른가. 초록에 민감한 사람 눈에 맞춰 가중치를 준다.
     딱 맞으면 0, 가장 먼 짝이 484, 아무렇게나 놓았을 때 평균 217 이다 */
  function _차(c, t) {
    return Math.sqrt(2*Math.pow(c[0]-t[0],2) + 4*Math.pow(c[1]-t[1],2) + 3*Math.pow(c[2]-t[2],2));
  }
  function 색차(geno, biomeKey) {
    return _차(몸빛깔(geno), _hex(바이옴(biomeKey).bg));
  }
  /* 겉모습 문자열만 갖고도 빛깔을 되살릴 수 있어야 한다 —
     남의 개체는 유전자형이 안 오고 겉모습만 온다 */
  function 겉번호(ph) { return Math.max(0, Math.min(8, +ph.charAt(PH.색) || 0)); }
  function 겉빛깔(ph) { return _hex(색표[겉번호(ph)].hex); }
  function 겉색이름(ph) { return 색표[겉번호(ph)].n; }

  function 표현형(geno, key, biomeKey) {
    var i = 자리(key);
    if (i < 0) return 0;
    var v = +geno.charAt(i);
    if (GENES[i].조건부) {
      /* 잡종은 지금 있는 곳에서 더 쓸모 있는 쪽으로 자란다.
         어디인지 모르면(옛 코드가 부를 때) 우성 쪽으로 둔다 */
      if (v === 1) {
        var b = biomeKey ? 바이옴(biomeKey) : null;
        return (b && b.앞다리 === 'fin') ? 0 : 1;
      }
      return v === 2 ? 1 : 0;
    }
    return v >= 1 ? 1 : 0;
  }

  /* 겉모습 — «보이는» 형질만. 숨은 형질은 여기 들어가지 않는다.
     색은 아홉 빛깔 가운데 몇 번인지 한 글자로 나간다 — 남에게 색
     유전자형까지 알려 줄 이유가 없다. 눈에 보이는 것은 빛깔 하나뿐이니까. */
  var PH = { fur:0, 색:1, eye:2, leg:3, tall:4, water:5, grip:6, limb:7, ant:8,
             scale:9, glow:10, wing:11, gill:12, horn:13, leaf:14, dig:15, echo:16 };
  var PH순 = ['fur','색','eye','leg','tall','water','grip','limb','ant',
              'scale','glow','wing','gill','horn','leaf','dig','echo'];

  function 표현형문자열(geno, biomeKey) {
    var s='';
    for (var i=0;i<PH순.length;i++) {
      var k = PH순[i];
      s += (k === '색') ? String(색번호(geno)) : String(표현형(geno, k, biomeKey));
    }
    return s;
  }

  function 유전자표기(geno, i) {
    var G=GENES[i], S=G.s.toUpperCase(), s=G.s.toLowerCase(), v=+geno.charAt(i);
    return v===2 ? S+S : v===1 ? S+s : s+s;
  }

  /* 처음 나눠 주는 유전자 — «새 형질» 자리는 반드시 0 이다.
     아무도 갖고 있지 않은 것이 돌연변이로 생겨야 의미가 있다 */
  function 무작위유전자() {
    var s='';
    for (var i=0;i<NG;i++) s += GENES[i].새형질 ? '0' : String(Math.floor(Math.random()*3));
    return s;
  }
  /* 학생이 DNA 로 손댈 수 있는 자리인가 (새 형질은 못 켠다) */
  function 고칠수있나(pos1) { return pos1>=1 && pos1<=NG && !GENES[pos1-1].새형질; }
  /* 이 개체가 가진 새 형질들 */
  function 가진새형질(geno) {
    var out=[];
    for (var i=0;i<새형질들.length;i++) {
      var G=새형질들[i];
      if (+geno.charAt(자리(G.k)) >= 1) out.push(G);
    }
    return out;
  }

  /* ── 보드 ── */
  function 가까운바이옴(t, m) {
    var best=BIOMES[0], bd=1e9;
    for (var i=0;i<BIOMES.length;i++) {
      var b=BIOMES[i], d=(t-b.t)*(t-b.t)+(m-b.m)*(m-b.m);
      if (d<bd) { bd=d; best=b; }
    }
    return best;
  }
  function 바이옴(key) {
    for (var i=0;i<BIOMES.length;i++) if (BIOMES[i].key===key) return BIOMES[i];
    return BIOMES[0];
  }
  /* 기후로 판을 만들고, 재해로 바뀐 칸(terrain)을 덮어씌운다.
     재해로 바다가 된 칸은 기후가 바뀌어도 바다로 남는다 */
  function 보드만들기(env, terrain) {
    var g=[];
    for (var r=0;r<5;r++) { g[r]=[];
      for (var c=0;c<5;c++) {
        var k = 가까운바이옴(c/4+(env.dt||0), r/4+(env.dm||0)).key;
        if (terrain) {
          var o = terrain[r+','+c];
          if (o) k = o;      // 재해로 굳어진 지형이 기후를 이긴다
        }
        g[r][c]=k;
      }
    }
    return g;
  }

  /* ── 적응도 · 생존 확률 ─────────────────────────────────────
     ★ 이 계산은 학생 화면에 숫자로 나가지 않는다 (정찰을 사야 보인다) */
  function 적응도(geno, biomeKey) {
    var b=바이옴(biomeKey), s=50, k, w;

    /* ① 보통 형질 — 맞으면 +, 틀리면 − */
    for (k in b.fav) {
      w = (b.w && b.w[k]) || 1;
      s += (표현형(geno,k,biomeKey)===b.fav[k] ? 12 : -12) * w;
    }

    /* ② 색 — 배경 «빛깔» 과 얼마나 가까운가 (위장).
       밝기 한 축이 아니라 실제 색으로 잰다. 그래서 파란 바다와 모래빛
       사막이 서로 다른 답을 요구한다.
       아무 색이 아무 구역에 있을 때 차이가 평균 217 이다.
       그 언저리를 본전으로 잡아, 맞추면 벌고 어긋나면 잃게 한다.
         차이   0 → +26   (그 구역에 «딱 맞는» 색)
         차이 217 → 0     (본전)
         차이 484 → -32   (가장 먼 색)
       빛깔(새 형질)이 나오면 위장이 아예 안 되므로 이 점수를 건너뛴다 */
    if (!표현형(geno,'glow')) {
      s += (217 - 색차(geno, biomeKey)) * 0.12;
    }

    /* ③ 새 형질 — «있을 때만» 더한다.
       없다고 깎으면 거의 모든 개체가 손해를 봐서 판이 흔들린다 */
    for (var i=0;i<새형질들.length;i++) {
      var G=새형질들[i];
      if (표현형(geno,G.k)) s += (b.새 && b.새[G.k]) || 0;
    }

    /* ④ 잡종 강세 */
    for (var hk in HET) {
      var hi=자리(hk);
      if (hi>=0 && geno.charAt(hi)==='1') s += HET[hk];
    }

    /* ⑤ 0~100 으로 담는다 — 자르지 않고 «눌러» 담는다.
       예전에는 0 아래·100 위를 뚝 잘랐는데, 무작위 개체 4000마리를 재 보니
       24%가 잘리고 있었다. 잘린 개체는 색을 맞추든 형질을 고치든 생존
       확률이 한 치도 안 바뀐다 — 네 마리 중 한 마리에게는 이 게임의 규칙이
       통째로 먹히지 않았던 셈이다.
       가운데(50)를 지나는 S자로 누르면 어디서든 «조금 나아지면 조금 더 산다».
       25로 나눈 것은, 개체 대부분이 몰려 있는 40~90 구간에서 예전 값과
       거의 같게 나오도록 맞춘 값이다 (39→39 · 66→66 · 93→85).
       그래서 지금까지 맞춰 놓은 균형은 그대로 두고 «잘림» 만 없앤다. */
    return Math.round(100 / (1 + Math.exp(-(s - 50) / 25)));
  }

  function 생존확률(geno, biomeKey, st, 넘침) {
    var p = st.base + (적응도(geno,biomeKey)/100) * st.span;
    if (표현형(geno,'life')===1) p -= st.lifeCost;
    p -= (넘침||0) * st.crowd;
    return Math.max(0.03, Math.min(0.95, p));
  }

  /* ── 멘델 ── */
  function 대립(v) { return v===2 ? 1 : v===0 ? 0 : (Math.random()<0.5 ? 1 : 0); }
  /* 자손의 유전자.
     보통 자리는 부모에게서 하나씩 받고, 아주 낮은 확률로 돌연변이가 난다.

     «새 형질» 자리는 다르다. 부모 둘 다 없으면(=0) 자손도 0 이 되는데,
     여기서만 아주 드물게 «없던 것이 생긴다». 한 개(잡종)로 생기고 우성이라
     그 자리에서 바로 눈에 보인다. 그 다음부터는 멘델의 법칙대로 퍼진다.
     — 변이가 먼저 생기고 그 다음에 환경이 고른다는 것을 눈으로 보는 자리다.
     보통 돌연변이율(2%)을 여기에 쓰면 한 세대에 수십 마리가 생겨 버려서
     «드문 일» 이 아니게 된다. 그래서 자리를 나눴다. */
  function 자손유전자(g1, g2, mut, novelMut, 물려줄새형질) {
    var out='', 새로생김=null;
    for (var i=0;i<NG;i++) {
      var v = 대립(+g1.charAt(i)) + 대립(+g2.charAt(i));
      if (GENES[i].새형질) {
        /* 돌연변이는 어버이의 «생식세포» 에서 일어난다. 그래서 한 번 일어나면
           그 어버이가 이번에 낳는 새끼가 모두 물려받는다(물려줄새형질).
           한 마리만 갖고 태어나면 거의 언제나 그 한 마리가 죽으면서 끝나
           «생겼다가 사라졌다» 만 남는다. 두세 마리가 함께 태어나야
           퍼지는 것을 볼 기회가 생긴다 */
        if (v === 0 && 물려줄새형질 === GENES[i].k) { v = 1; 새로생김 = GENES[i]; }
      } else if (mut && Math.random() < mut) {
        v = Math.floor(Math.random()*3);
      }
      out += String(v);
    }
    return 새로생김 ? { g:out, 새형질:새로생김.k, 이름:새로생김.n, emoji:새로생김.emoji }
                    : out;
  }
  /* 이번 교배에서 «없던 형질» 이 하나 생길까? 생긴다면 어느 것일까.
     한배 새끼를 만들기 전에 딱 한 번 굴린다 */
  function 새형질굴리기(g1, g2, novelMut) {
    if (!novelMut) return null;
    /* 형질마다 따로 굴린 뒤, 맞은 것 가운데 하나를 무작위로 고른다.
       예전에는 «맨 처음 맞은 것» 을 돌려주어서, 목록 앞쪽 형질이
       늘 먼저 나왔다. 형질이 셋일 때는 티가 안 났지만 여덟이면 크다 */
    var 후보 = [];
    for (var i=0;i<새형질들.length;i++) {
      var G=새형질들[i], j=자리(G.k);
      // 둘 다 안 갖고 있을 때만 «없던 것이 생긴다»
      if (+g1.charAt(j)===0 && +g2.charAt(j)===0 && Math.random() < novelMut) 후보.push(G.k);
    }
    if (!후보.length) return null;
    return 후보[Math.floor(Math.random()*후보.length)];
  }
  /* 옛 코드가 문자열을 기대하므로, 어느 쪽이 와도 문자열로 만들어 주는 도우미 */
  function 유전자만(x) { return (typeof x === 'string') ? x : x.g; }

  /* 자손 수 — 2 아니면 4.
     번식력은 «우열» 이 아니라 대립유전자가 쌓이는 만큼 확률이 오른다.
     그래서 잡종(Rr)도 순종열성(rr)보다 낫다 — 우열의 법칙과 다른 유전 방식이다 */
  function 넷확률(geno, st) {
    var v = +geno.charAt(자리('fec'));
    return v===2 ? st.fec2 : v===1 ? st.fec1 : st.fec0;
  }
  function 자손수(geno, st) {
    return Math.random() < 넷확률(geno, st) ? st.kid4 : st.kid2;
  }

  /* ── 환경 변화 ── */
  var 변화목록 = [
    { k:'warm', 설명:'행성이 더워졌습니다 (온난화)', dt: 1, dm: 0 },
    { k:'cold', 설명:'행성이 추워졌습니다 (한랭화)', dt:-1, dm: 0 },
    { k:'wet',  설명:'비가 늘었습니다 (습윤화)',     dt: 0, dm: 1 },
    { k:'dry',  설명:'비가 줄었습니다 (건조화)',     dt: 0, dm:-1 },
    { k:'keep', 설명:'큰 변화가 없었습니다',         dt: 0, dm: 0 }
  ];
  function 변화찾기(k) {
    for (var i=0;i<변화목록.length;i++) if (변화목록[i].k===k) return 변화목록[i];
    return null;
  }
  var 환경한계 = 0.50;     // 판이 끝까지 밀리면 두 칸까지 간다

  /* 지금 환경(env)을 주면 «벽에 막히지 않는» 변화만 고른다.
     안 주면 예전처럼 넷 중 하나를 그냥 고른다.
     이걸 안 하면 판이 끝까지 밀린 뒤로는 예보만 나오고 아무 칸도
     안 바뀌는 세대가 다섯에 한 번씩 생긴다 — 예보가 거짓말이 된다 */
  function 무작위변화(env, st) {
    if (!env) return 변화목록[Math.floor(Math.random()*4)];
    var s = (st && st.shift) || 0.25, 가능 = [];
    for (var i=0;i<4;i++) {
      var v = 변화목록[i];
      if (Math.abs((env.dt||0) + v.dt*s) <= 환경한계 + 1e-9 &&
          Math.abs((env.dm||0) + v.dm*s) <= 환경한계 + 1e-9) 가능.push(v);
    }
    if (!가능.length) 가능 = 변화목록.slice(0,4);
    return 가능[Math.floor(Math.random()*가능.length)];
  }

  function 환경적용(env, 변화, st) {
    var lim=환경한계, s=st.shift;
    return {
      dt: Math.max(-lim, Math.min(lim, (env.dt||0) + 변화.dt*s)),
      dm: Math.max(-lim, Math.min(lim, (env.dm||0) + 변화.dm*s))
    };
  }

  /* ══ 재해 ══════════════════════════════════════════════════════
     한 칸을 골라 그 둘레(범위)까지 덮친다.
     그 안의 개체는 «형질에 따라» 살아남는다. 지형이 바뀌기도 한다. */
  /* 사는 곳(biomeKey)을 같이 받는다 — 앞다리처럼 «사는 곳이 정하는» 형질이
     있어서, 어디서 재해를 맞았는지에 따라 답이 달라진다 */
  function 재해생존확률(geno, ev, biomeKey) {
    var p = ev.기본생존;
    for (var k in ev.살아남는) {
      var w = (ev.w && ev.w[k]) || 1;
      if (표현형(geno,k,biomeKey) === ev.살아남는[k]) p += 0.11 * w;
    }
    /* 새 형질이 재해에서 하는 몫은 형질 정의(재해·재해모두)에 적어 둔다.
       여기에 따로 박아 두면 형질을 더할 때마다 두 곳을 고쳐야 한다 */
    for (var i=0;i<새형질들.length;i++) {
      var G = 새형질들[i];
      if (!표현형(geno, G.k, biomeKey)) continue;
      p += G.재해모두 || 0;
      if (G.재해 && G.재해[ev.k]) p += G.재해[ev.k];
    }
    return Math.max(0.02, Math.min(0.96, p));
  }

  function 무작위재해() { return EVENTS[Math.floor(Math.random()*EVENTS.length)]; }
  function 재해찾기(k) {
    for (var i=0;i<EVENTS.length;i++) if (EVENTS[i].k===k) return EVENTS[i];
    return null;
  }

  /* 재해를 일으킨다. 어디를 덮칠지·누가 죽을지·지형이 어떻게 바뀔지 돌려준다 */
  function 재해발생(board, aliens, terrain, ev, 중심) {
    ev = ev || 무작위재해();
    var cr = 중심 ? 중심.r : Math.floor(Math.random()*5);
    var cc = 중심 ? 중심.c : Math.floor(Math.random()*5);

    // 중심에서 가까운 순으로 범위만큼 고른다
    var 후보=[], r, c;
    for (r=0;r<5;r++) for (c=0;c<5;c++)
      후보.push({ r:r, c:c, d:Math.max(Math.abs(r-cr), Math.abs(c-cc)) });
    /* 거리가 같으면 무작위로 — 그냥 정렬하면 «항상 위·왼쪽» 만 맞는다.
       실제로 재 보니 A2 가 E1 보다 5.5배 자주 맞았고, 맨 아랫줄이 안전지대가 됐다 */
    후보.forEach(function (x) { x.j = Math.random(); });
    후보.sort(function (a,b) { return (a.d - b.d) || (a.j - b.j); });
    var 칸들 = 후보.slice(0, ev.범위).map(function (x) { return { r:x.r, c:x.c }; });

    // 침강은 바다가 아닌 곳만, 융기는 바다인 곳만 뜻이 있다
    if (ev.k === 'sink')  칸들 = 칸들.filter(function (x) { return board[x.r][x.c] !== 'sea'; });
    if (ev.k === 'rise')  칸들 = 칸들.filter(function (x) { return board[x.r][x.c] === 'sea'; });
    if (!칸들.length) return null;         // 일어날 자리가 없다

    var 안에 = {}, i;
    for (i=0;i<칸들.length;i++) 안에[칸들[i].r+','+칸들[i].c] = 1;

    var 죽을것=[], 산것=0;
    for (i=0;i<aliens.length;i++) {
      var a = aliens[i];
      if (!a.alive || !안에[a.r+','+a.c]) continue;
      if (Math.random() < 재해생존확률(a.geno, ev, board[a.r][a.c])) 산것++;
      else 죽을것.push(a.id);
    }

    var 새terrain = {};
    for (var kk in (terrain||{})) 새terrain[kk] = terrain[kk];
    /* 융기는 «바다 표시를 지우는» 것만으로는 부족하다.
       기후 때문에 바다가 된 칸은 표시가 없어서 지울 것도 없기 때문이다.
       그래서 «땅으로 굳힌다» 는 표시를 따로 남긴다 */
    if (ev.지형) for (i=0;i<칸들.length;i++) {
      var key = 칸들[i].r+','+칸들[i].c;
      if (ev.지형 === 'rise') 새terrain[key] = 'rock';
      else 새terrain[key] = ev.지형;
    }

    return {
      사건:ev.k, 이름:ev.n, emoji:ev.emoji, 말:ev.말,
      칸들:칸들, 죽을것:죽을것, 살아남음:산것, 죽음:죽을것.length,
      terrain:새terrain
    };
  }

  /* ── 칸마다 몇 마리인가 ── */
  function 몰림세기(aliens) {
    var m={};
    for (var i=0;i<aliens.length;i++) {
      var a=aliens[i];
      if (!a.alive) continue;
      var k=a.r+','+a.c; m[k]=(m[k]||0)+1;
    }
    return m;
  }

  /* ══ 단계 계산 — 선생님 화면이 부른다 ══════════════════════════ */

  function 생존판정(aliens, board, st) {
    var 몰림 = 몰림세기(aliens), 죽음=[], 산것=0;
    var 최다=0, 최다칸='';
    for (var k in 몰림) if (몰림[k]>최다) { 최다=몰림[k]; 최다칸=k; }

    for (var i=0;i<aliens.length;i++) {
      var a=aliens[i];
      if (!a.alive) continue;
      var 넘침 = Math.max(0, (몰림[a.r+','+a.c]||0) - st.cellCap);
      if (Math.random() < 생존확률(a.geno, board[a.r][a.c], st, 넘침)) 산것++;
      else 죽음.push(a.id);
    }
    return { 단계:'생존 판정', 죽을것:죽음, 살아남음:산것, 죽음:죽음.length,
             붐빈수:최다, 넉넉한수:st.cellCap, 가장붐빈칸:최다칸 };
  }

  function 자동교배(aliens, 이미짝지은, st) {
    var 남은=[], i;
    for (i=0;i<aliens.length;i++) {
      var a=aliens[i];
      if (a.alive && !이미짝지은[a.id]) 남은.push(a);
    }
    for (i=남은.length-1;i>0;i--) {
      var j=Math.floor(Math.random()*(i+1)), t=남은[i]; 남은[i]=남은[j]; 남은[j]=t;
    }
    var 쓴것={}, 짝=[], 실패=0;
    for (i=0;i<남은.length;i++) {
      var A=남은[i];
      if (쓴것[A.id]) continue;
      var 찾음=null;
      for (var y=0;y<남은.length;y++) {
        var B=남은[y];
        if (쓴것[B.id] || B.id===A.id) continue;
        var d=Math.max(Math.abs(A.r-B.r), Math.abs(A.c-B.c));
        if (st.autoScope==='near' ? d<=1 : d===0) { 찾음=B; break; }
      }
      if (찾음) { 쓴것[A.id]=1; 쓴것[찾음.id]=1;
                  짝.push({ a:A, b:찾음, same:A.player===찾음.player }); }
      else { 실패++; }
    }
    return { 짝:짝, 성사:짝.length, 실패:실패 };
  }

  function 세대교체(aliens, 짝목록, players, turn, st) {
    var 새것=[], 자손=0, 사망=0, 장수=0, 구제=0, 넷쌍=0;
    var 새소식=[];   // 이번 세대에 «없던 형질» 이 생긴 기록
    var 사람별={}, i;
    for (i=0;i<players.length;i++) 사람별[players[i].id] = { p:players[i], list:[] };

    for (i=0;i<aliens.length;i++) {
      var a=aliens[i];
      if (!a.alive) { 사망++; continue; }
      if (표현형(a.geno,'life')===1 && (a.age||0) < st.lifeExtra) {
        var 통=사람별[a.player];
        if (통) { 통.list.push({ player:a.player, geno:a.geno, r:a.r, c:a.c,
                                 alive:true, born:a.born, age:(a.age||0)+1 }); 장수++; }
      } else { 사망++; }
    }

    for (i=0;i<짝목록.length;i++) {
      var 짝=짝목록[i], A=짝.a, B=짝.b;
      if (짝.same) {
        var n = Math.max(2, 자손수(A.geno, st) - st.inbreed);
        자손 += _낳기(사람별, A.player, A, B, n, turn, st, 새소식);
      } else {
        var nA = 자손수(A.geno, st), nB = 자손수(B.geno, st);
        if (nA === st.kid4) 넷쌍++;
        if (nB === st.kid4) 넷쌍++;
        자손 += _낳기(사람별, A.player, A, B, nA, turn, st, 새소식);
        자손 += _낳기(사람별, B.player, B, A, nB, turn, st, 새소식);
      }
    }

    for (var pid in 사람별) {
      var 통2=사람별[pid], L=통2.list;
      if (L.length > st.cap) {
        for (var s=L.length-1;s>0;s--) { var t2=Math.floor(Math.random()*(s+1)),
          tmp=L[s]; L[s]=L[t2]; L[t2]=tmp; }
        L.length = st.cap;
      }
      if (L.length===0 && st.rescue>0) {
        var 집 = { r: 통2.p.home_r==null?2:통2.p.home_r, c: 통2.p.home_c==null?2:통2.p.home_c };
        for (var q=0;q<st.rescue;q++)
          L.push({ player:+pid, geno:무작위유전자(), r:집.r, c:집.c,
                   alive:true, born:turn+1, age:0 });
        구제++;
      }
      새것 = 새것.concat(L);
    }
    return { 단계:'세대 교체', 개체:새것, 세대:turn+1,
             자손:자손, 사망:사망, 장수생존:장수, 구제:구제, 네마리:넷쌍,
             새형질:새소식 };
  }

  function _낳기(사람별, pid, 나, 짝, n, turn, st, 새소식) {
    var 통=사람별[pid];
    if (!통) return 0;
    var 만듦=0;
    // 한배 새끼가 함께 물려받는다 — 굴리기는 한 번뿐이다
    var 새것 = 새형질굴리기(나.geno, 짝.geno, st.novelMut);
    for (var i=0;i<n;i++) {
      if (통.list.length >= st.cap) break;
      var x = 자손유전자(나.geno, 짝.geno, st.mut, st.novelMut, 새것);
      if (typeof x !== 'string' && 새소식 && i === 0) {
        // 없던 형질이 생겼다. 누구에게서 어디서 생겼는지 적어 둔다
        새소식.push({ 형질:x.새형질, 이름:x.이름, emoji:x.emoji,
                      player:pid, r:나.r, c:나.c, 마리:n });
      }
      통.list.push({ player:pid, geno:유전자만(x),
                     r:나.r, c:나.c, alive:true, born:turn+1, age:0 });
      만듦++;
    }
    return 만듦;
  }

  /* ── 세대별 형질 빈도 (그래프 재료) ──
     대립유전자 빈도로 잰다 — 겉모습보다 유전자 풀의 변화를 잘 보여 준다 */
  function 빈도(aliens) {
    var n=0, dom={}, i;
    for (i=0;i<NG;i++) dom[GENES[i].k]=0;
    for (i=0;i<aliens.length;i++) {
      var a=aliens[i];
      if (!a.alive) continue;
      n++;
      for (var k=0;k<NG;k++) dom[GENES[k].k] += +a.geno.charAt(k);
    }
    var f={};
    for (i=0;i<NG;i++) f[GENES[i].k] = n ? Math.round(dom[GENES[i].k]/(n*2)*100) : 0;
    return { n:n, f:f };
  }

  /* 더듬이가 긴 개체를 하나라도 갖고 있으면 다음 재해 경보를 볼 수 있다 */
  function 경보볼수있나(내개체들) {
    for (var i=0;i<내개체들.length;i++) {
      var a = 내개체들[i];
      if (a.alive && 표현형(a.geno,'ant') === 1) return true;
    }
    return false;
  }

  /* 구역별 유전자 풀 — 빔 화면이 쓴다 */
  function 구역빈도(aliens, r, c) {
    var 안=[];
    for (var i=0;i<aliens.length;i++) {
      var a=aliens[i];
      if (a.alive && a.r===r && a.c===c) 안.push(a);
    }
    return 빈도(안);
  }

  전역.RULES = {
    GENES:GENES, NG:NG, 보이는수:보이는수, BIOMES:BIOMES, HET:HET, EVENTS:EVENTS,
    PHASES:PHASES, PHASE_NAME:PHASE_NAME,
    색자리:색자리, 색표:색표, 새형질들:새형질들, PH:PH, PH순:PH순,
    색번호:색번호, 빛깔:빛깔, 몸빛깔:몸빛깔, 색차:색차,
    겉번호:겉번호, 겉빛깔:겉빛깔, 겉색이름:겉색이름,
    기본설정:기본설정, 자리:자리, 표현형:표현형, 표현형문자열:표현형문자열,
    고칠수있나:고칠수있나, 가진새형질:가진새형질, 유전자만:유전자만,
    유전자표기:유전자표기, 무작위유전자:무작위유전자,
    바이옴:바이옴, 보드만들기:보드만들기, 적응도:적응도, 생존확률:생존확률,
    자손유전자:자손유전자, 새형질굴리기:새형질굴리기, 자손수:자손수, 넷확률:넷확률, 몰림세기:몰림세기,
    변화목록:변화목록, 변화찾기:변화찾기, 무작위변화:무작위변화, 환경적용:환경적용,
    무작위재해:무작위재해, 재해찾기:재해찾기, 재해발생:재해발생, 재해생존확률:재해생존확률,
    생존판정:생존판정, 자동교배:자동교배, 세대교체:세대교체, 빈도:빈도, 구역빈도:구역빈도,
    경보볼수있나:경보볼수있나
  };
})(window);
